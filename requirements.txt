fastapi
uvicorn[standard]
python-docx
chromadb
# sentence-transformers is NOT needed on Railway — the vector DB is pre-built
# and ChromaDB uses its own lightweight ONNX runtime for query embedding.
# Only needed locally when re-running ingest/ingest.py to rebuild the DB.
google-genai
python-dotenv
pydantic
